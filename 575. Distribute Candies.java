class Solution {
    public int distributeCandies(int[] candyType) {
        int n = candyType.length2;
        SetInteger map = new HashSet();
        for(int numcandyType){
            map.add(num);
        }
        int count = map.size();
        
        if (count = n) {
            return n;
        } else {
            return count;
        }
    }
}